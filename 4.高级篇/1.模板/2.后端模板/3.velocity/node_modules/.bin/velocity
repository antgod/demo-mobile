#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../velocityjs/bin/velocity" "$@"
  ret=$?
else 
  node  "$basedir/../velocityjs/bin/velocity" "$@"
  ret=$?
fi
exit $ret
